name: angle_calc
description: A pure Dart library for calculating angles between vectors, lines, and points. Useful for AR, geometry, and measurement apps.
version: 0.1.0
homepage: https://github.com/siddhi8907/ar_protractor

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.24.0