name: ar_angle_painter
description: A Flutter CustomPainter widget for visualizing angle measurements between two lines.
version: 0.1.0
homepage: https://github.com/siddhi8907/ar_protractor

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

